<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Offer Management</title>
    <style>
        /* General Styles */
        body { 
            font-family: Arial, sans-serif; 
            margin: 0;
            padding: 0;
            display: flex;
            min-height: 100vh;
            background-color: #f5f5f5;
        }
        
        /* Sidebar Styles */
        .sidebar {
            width: 215px;
            background-color: #333;
            color: white;
            padding: 20px;
            height: 100vh;
            position: fixed;
            box-shadow: 2px 0 5px rgba(0,0,0,0.1);
            z-index: 10; /* Higher than main content */
        }
        
        .sidebar h2 {
            text-align: center;
            color: white;
            margin-bottom: 30px;
            padding-bottom: 10px;
            border-bottom: 1px solid #444;
        }

        .sidebar ul {
            list-style-type: none;
            padding: 0;
            margin: 0;
        }

        .sidebar ul li {
            margin: 15px 0;
        }

        .sidebar ul li a {
            color: #fff;
            text-decoration: none;
            display: block;
            padding: 8px 10px;
            border-radius: 4px;
            transition: background-color 0.3s;
        }

        .sidebar ul li a:hover {
            background-color: #444;
        }
        
        /* Main Content Styles */
        .main-content {
            flex: 1;
            margin-left: 250px;
            padding: 20px 20px 20px 30px;
            background-color: white;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
            position: relative;
            z-index: 1;
        }
        
        .header-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }
        
        .heading {
            flex-grow: 1;
            text-align: center;
            color: #333;
        }
        
        /* Table Container Styles */
        .table-container {
            max-height: 500px;
            overflow-y: auto;
            margin-top: 20px;
            border: 1px solid #ddd;
            border-radius: 5px;
            position: relative;
            z-index: 1;
        }
        
        /* Table Styles */
        table {
            width: 100%;
            border-collapse: collapse;
        }
        
        th, td {
            padding: 12px;
            text-align: left;
            border: 1px solid #ddd;
        }
        
        th {
            background-color: #333;
            color: #fff;
            font-weight: bold;
            border: 1px solid #444;
            position: sticky;
            top: 0;
            z-index: 5;
        }
        
        tr:nth-child(even) {
            background-color: #f9f9f9;
        }
        
        tr:hover {
            background-color: #f1f1f1;
        }
        
        /* Button Styles */
        .btn {
            padding: 6px 12px;
            border-radius: 4px;
            font-size: 14px;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
            text-align: center;
            transition: background-color 0.3s;
            border: none;
        }
        
        .btn-success {
            background-color: #4CAF50;
            color: white;
        }
        
        .btn-danger {
            background-color: #f44336;
            color: white;
        }
        
        .btn-primary {
            background-color:green;;
            color: white;
            padding: 8px 15px;
            font-weight: bold;
            margin-bottom: 20px;
        }
        
        .btn-primary:hover {
           background-color: #218838;
        }
        
        /* Action Button Styles */
        .action-buttons {
            display: flex;
            gap: 5px;
            min-width: 100px;
        }
        
        .btn-edit1, .btn-edit2 {
            color: #f2f2f2;
            font-weight: bold;
            padding: 6px 12px;
            border-radius: 4px;
            text-decoration: none;
            text-align: center;
            box-sizing: border-box;
            white-space: nowrap;
        }
        
        .btn-edit1 {
            background-color: #2196F3;
        }
        
        .btn-edit2 {
            background-color: #f44336;
        }
        
        /* Update the table cell to prevent text wrapping */
        td {
            white-space: nowrap;
            vertical-align: middle;
        }
        
        /* Responsive Adjustments */
        @media (max-width: 768px) {
            .sidebar {
                width: 100%;
                height: auto;
                position: relative;
            }
            
            .main-content {
                margin-left: 0;
            }
            
            .action-buttons {
                flex-direction: row;
                flex-wrap: nowrap;
            }
            
            .btn-edit1, .btn-edit2 {
                font-size: 12px;
                padding: 4px 8px;
            }
        }
    </style>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.css">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</head>
<body>
    <!-- Sidebar -->
    <div class="sidebar">
        <h2>Admin Panel</h2>
        <ul>
            <li><a href="/admin/dashboard">Dashboard</a></li>
            <li><a href="/admin/usermanagement">User Management</a></li>
            <li><a href="/admin/categorymanagement">Category Management</a></li>
            <li><a href="/productmanagement">Product Management</a></li>
            <li><a href="/admin/orderManagement">Order Management</a></li>
            <li><a href="/couponManagement">Coupon Management</a></li>
            <li><a href="/salesReport">Sales</a></li>
            <li><a href="/offerManagement">Offer Management</a></li>
            <li><a href="/admin/adminLogin" id="logout-link">Logout</a></li>
        </ul>
    </div>

    <div class="main-content">
        <div class="header-container">
            <h2 class="heading">Offer Management</h2>
        </div>
        
        <button id="add-offer"  class="btn btn-primary">+ Add Product Offer</button>
        <div class="table-container">
            <table id="productoffer">
                <thead>
                    <tr>
                        <th>No</th>
                        <th>Product Name</th>
                        <th>Price</th>
                        <th>Offer</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                   <% if(productOffer && productOffer.length>0) { %>
                    <% productOffer.forEach((offer,index)=>{ %>
                      <tr>
                        <td><%= index+1 %></td>
                        <td><%=offer.productname %></td>
                        <td><%=offer.price %></td>
                        <td><%=offer.productoffer %>%</td>
                        <td>
                            <div class="action-buttons">
                                <a href="/deleteProductOffer/<%=offer.id%>" class="btn-edit2">Delete</a>
                            </div>
                        </td>
                      </tr>
                    <%})%>
                    <%}%>
                </tbody>
            </table>
        </div>

        <button id="addcategory-offer" class="btn btn-primary" style="margin-top: 30px;">+ Add Category Offer</button>
        <div class="table-container">
            <table id="addcategoryoffer">
                <thead>
                    <tr>
                        <th>Category Name</th>
                        <th>Offer</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    <% if(categoryOffer && categoryOffer.length>0) { %>
                        <% categoryOffer.forEach((categoryOffers)=>{ %>
                            <tr>
                                <td><%=categoryOffers.category.name %></td>
                                <td><%=categoryOffers.alloffer %>%</td>
                                <td>
                                    <div class="action-buttons">
                                        <a href="/deleteCategoryOffer/<%=categoryOffers.id%>" class="btn-edit2">Delete</a>
                                    </div>
                                </td>
                            </tr>
                        <%})%>
                    <%}%>
                </tbody>
            </table>
        </div>
    </div>

    <script>
        document.getElementById('logout-link').addEventListener('click', function (e) {
            e.preventDefault(); // Prevent the default link behavior

            Swal.fire({
                title: "Are you sure you want to logout?",
                icon: "question",
                showCancelButton: true,
                confirmButtonText: "Yes, logout",
                cancelButtonText: "Cancel",
            }).then((result) => {
                if (result.isConfirmed) {
                    // Redirect to the logout route
                    window.location.href = "/admin/adminLogin";
                }
            });
        });
        
        document.getElementById('add-offer').addEventListener('click',function(){
            window.location.href='/addProductOffer'
        });
        
        document.getElementById('addcategory-offer').addEventListener('click',function(){
            window.location.href='/addCategoryOffer'
        }); 
    </script>
</body>
</html>