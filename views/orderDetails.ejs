<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Order Details</title>
  <!-- Bootstrap 5 CSS CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <!-- Bootstrap Icons CDN -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css"/>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
  <style>
    :root {
      --primary-color: #3498db;
      --primary-hover: #2980b9;
      --success-color: #27ae60;
      --warning-color: #f39c12;
      --danger-color: #e74c3c;
      --dark-color: #2c3e50;
      --light-color: #f8f9fa;
      --gray-color: #7f8c8d;
      --border-color: #eee;
    }
    
    body {
      background-color: #f5f7fa;
      color: #333;
      line-height: 1.6;
      padding: 20px;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .order-container {
      max-width: 1200px;
      margin: 0 auto;
      background: white;
      border-radius: 12px;
      box-shadow: 0 5px 30px rgba(0, 0, 0, 0.08);
      padding: 40px;
      overflow: hidden;
    }

    .order-header {
      border-bottom: 1px solid var(--border-color);
      padding-bottom: 20px;
      margin-bottom: 30px;
      position: relative;
    }

    .order-header::after {
      content: '';
      position: absolute;
      bottom: -1px;
      left: 0;
      width: 100px;
      height: 3px;
      background: var(--primary-color);
    }

    .order-title {
      color: var(--dark-color);
      margin-bottom: 10px;
      font-weight: 700;
      letter-spacing: -0.5px;
    }

    .order-meta {
      color: var(--gray-color);
      font-size: 0.9em;
      display: flex;
      gap: 15px;
      flex-wrap: wrap;
      align-items: center;
    }

    .order-meta .badge {
      font-size: 0.75em;
      font-weight: 600;
      padding: 5px 10px;
      border-radius: 50px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .invoice-btn {
      background: var(--primary-color);
      color: white;
      transition: all 0.3s ease;
      font-weight: 500;
      padding: 8px 16px;
      border-radius: 6px;
      display: inline-flex;
      align-items: center;
      gap: 5px;
    }

    .invoice-btn:hover {
      background: var(--primary-hover);
      color: white;
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(52, 152, 219, 0.3);
    }

    .product-img {
      width: 80px;
      height: 80px;
      object-fit: cover;
      border-radius: 8px;
      border: 1px solid var(--border-color);
      transition: transform 0.3s ease;
    }

    .product-img:hover {
      transform: scale(1.05);
    }

    .payment-badge {
      background-color: var(--success-color);
      font-size: 0.75em;
      padding: 4px 8px;
      border-radius: 4px;
      margin-left: 8px;
    }

    .section-card {
      background: white;
      border-radius: 10px;
      box-shadow: 0 3px 15px rgba(0, 0, 0, 0.03);
      padding: 25px;
      margin-bottom: 25px;
      border: none;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      border-left: 4px solid var(--primary-color);
    }

    .section-card:hover {
      transform: translateY(-3px);
      box-shadow: 0 5px 20px rgba(0, 0, 0, 0.08);
    }

    .section-title {
      color: var(--dark-color);
      padding-bottom: 10px;
      border-bottom: 1px solid var(--border-color);
      margin-bottom: 20px;
      font-weight: 600;
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .section-title i {
      color: var(--primary-color);
    }

    .tracking-code {
      color: var(--primary-color);
      font-weight: bold;
      text-decoration: none;
      transition: all 0.3s ease;
      display: inline-flex;
      align-items: center;
      gap: 5px;
    }

    .tracking-code:hover {
      color: var(--primary-hover);
      text-decoration: underline;
    }

    .store-address strong {
      color: var(--danger-color);
      font-size: 1.1em;
    }

    .status-processing {
      background-color: var(--warning-color);
    }

    .status-shipped {
      background-color: var(--primary-color);
    }

    .status-delivered {
      background-color: var(--success-color);
    }

    .status-cancelled {
      background-color: var(--danger-color);
    }

    table {
      border-collapse: separate;
      border-spacing: 0;
    }

    table tbody tr:last-child td {
      border-bottom: 1px solid var(--border-color);
    }

    table tfoot tr:first-child td {
      border-top: 2px solid var(--border-color);
    }

    table tfoot tr:last-child td {
      font-size: 1.1em;
      color: var(--dark-color);
    }

    .table-group-divider td {
      padding-top: 15px;
      padding-bottom: 15px;
    }

    .dropdown-menu {
      border-radius: 8px;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
      border: none;
      padding: 8px;
    }

    .dropdown-item {
      border-radius: 6px;
      padding: 8px 12px;
      font-size: 0.9em;
      transition: all 0.2s ease;
    }

    .dropdown-item:hover {
      background-color: var(--primary-color);
      color: white;
    }

    .dropdown-item i {
      width: 20px;
      text-align: center;
    }

    address {
      font-style: normal;
      line-height: 1.7;
    }

    @media (max-width: 768px) {
      .order-container {
        padding: 20px;
      }

      .shipping-info {
        flex-direction: column;
        align-items: flex-start !important;
      }

      .shipping-info .mt-md-0 {
        margin-top: 15px !important;
      }
      
      .order-header {
        padding-bottom: 15px;
      }
      
      .section-card {
        padding: 20px;
      }
    }
  </style>
</head>
<body>
  <div class="order-container">
    <div class="order-header">
      <div class="d-flex justify-content-between align-items-start flex-wrap">
        <div>
          <h2 class="order-title">Order #<%= orderid.substring(0, 7) %></h2>
          <div class="order-meta d-flex gap-3 flex-wrap mt-2">
            <span><i class="bi bi-calendar me-1"></i><%= new Date(order.orderDate).toLocaleDateString() %></span>
            <span><i class="bi bi-credit-card me-1"></i><%= order.paymentMethod %></span>
            <span class="badge status-<%= product.status.toLowerCase() %>"><i class="bi bi-circle-fill me-1"></i><%= product.status %></span>
          </div>
        </div>
        <div class="d-flex gap-2 mt-2 mt-md-0">
          <a href="/Invoice/<%=order._id %>" class="btn btn-sm invoice-btn"><i class="bi bi-file-earmark-text"></i> Invoice</a>
          <div class="dropdown">
            <button class="btn btn-sm btn-outline-secondary" type="button" data-bs-toggle="dropdown">
              <i class="bi bi-three-dots-vertical"></i>
            </button>
            <ul class="dropdown-menu dropdown-menu-end">
              <li><a class="dropdown-item" href="#"><i class="bi bi-pencil me-2"></i>Edit</a></li>
              <li><a class="dropdown-item" href="#"><i class="bi bi-printer me-2"></i>Print</a></li>
              <li><a class="dropdown-item" href="#"><i class="bi bi-share me-2"></i>Share</a></li>
            </ul>
          </div>
        </div>
      </div>
    </div>

    <div class="table-responsive mb-4">
      <table class="table">
        <tbody>
          <tr>
            <td class="border-0">
              <div class="d-flex align-items-center">
                <img src="/<%= product.image[0] %>" alt="<%= product.productname %>" class="product-img me-3" />
                <div>
                  <h6 class="mb-0"><%= product.productname %></h6>
                  <small class="text-muted">Quantity: <%= product.quantity %></small>
                </div>
              </div>
            </td>
          </tr>
        </tbody>
        <tfoot class="table-group-divider">
          <tr>
            <td>Subtotal</td>
            <td class="text-end">₹<%= orginalPrice * product.quantity %></td>
          </tr>
          <tr>
            <td>COUPON</td>
            <td class="text-end">-₹<%= discountAmount %></td>
          </tr>
          <tr>
            <td>Discount Offer</td>
            <td class="text-end">-₹<%= order.Discount %></td>
          </tr>
          <tr>
            <td>Tax</td>
            <td class="text-end">+₹<%= taxAmount %></td>
          </tr>
          <%
            taxAmount = Number(taxAmount);
            const totalAmount = (orginalPrice * product.quantity) - discountAmount - order.Discount + taxAmount;
          %>
          <tr class="fw-bold">
            <td>Total</td>
            <td class="text-end">₹<%= totalAmount %></td>
          </tr>
        </tfoot>
      </table>
    </div>

    <div class="row g-4">
      <div class="col-md-6">
        <div class="section-card h-100">
          <h4 class="section-title"><i class="bi bi-credit-card"></i> Payment Method</h4>
          <p><%= order.paymentMethod %></p>
          <% if (order.paymentMethod === 'Cash on Delivery') { %>
            <p>Total Price: ₹<%= totalAmount %> <span class="badge payment-badge">PAID</span></p>
          <% } %>
        </div>
      </div>
      
      <div class="col-md-6">
        <div class="section-card h-100">
          <div class="d-flex justify-content-between align-items-center flex-wrap shipping-info">
            <h4 class="section-title mb-3 mb-md-0"><i class="bi bi-truck"></i> Shipping Information</h4>
            <div class="mt-md-0">
              <strong>DTDC</strong>
              <a href="#" class="tracking-code ms-2" target="_blank"><i class="bi bi-box-seam"></i> TT1234567890</a>
            </div>
          </div>
        </div>
      </div>
      
      <div class="col-md-6">
        <div class="section-card h-100">
          <h4 class="section-title"><i class="bi bi-house-door"></i> Billing Address</h4>
          <p><%= order.addressCollection.firstname %> <%= order.addressCollection.lastname %></p>
          <p><%= order.addressCollection.address %></p>
          <p><%= order.addressCollection.city %>, <%= order.addressCollection.pincode %></p>
          <p><i class="bi bi-telephone me-1"></i> <%= order.addressCollection.phone %></p>
        </div>
      </div>
      
      <div class="col-md-6">
        <div class="section-card h-100">
          <address class="store-address">
            <h4 class="section-title"><i class="bi bi-shop"></i> Store Information</h4>
            <strong>RED STORE</strong><br />
            1345 Market st, suite 500<br />
            Kollam, KL 691501<br />
            <i class="bi bi-telephone me-1"></i> (+91) 9645342887
          </address>
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap 5 JS Bundle with Popper -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>